
  # Create Page Animations

  This is a code bundle for Create Page Animations. The original project is available at https://www.figma.com/design/35W3CeOO3mSDDkDVRxxPDG/Create-Page-Animations.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  